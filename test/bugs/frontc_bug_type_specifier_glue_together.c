typedef struct stree{
	int a;
} tree;
int
maybe_objc_comptypes (lhs, rhs, reflexive)
     tree lhs, rhs;
     int reflexive;
{
  return -1;
}
int f(void){
}

int
pushcase_range ( value2, converter, label, duplicate) 
     register tree value2;
     tree (*converter) (tree, tree);
     register tree label; 
     tree *duplicate;
{}
